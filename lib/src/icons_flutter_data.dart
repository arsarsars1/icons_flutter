import 'package:flutter/material.dart';

class IconsFlutterData extends IconData {
  const IconsFlutterData(int codePoint, String fontFamily)
      : super(codePoint, fontFamily: fontFamily, fontPackage: "flutter_icon");

  const IconsFlutterData.ionicons(int codePoint) : this(codePoint, "Ionicons");

  const IconsFlutterData.antDesign(int codePoint) : this(codePoint, "AntDesign");

  const IconsFlutterData.fontAwesome(int codePoint)
      : this(codePoint, "FontAwesome");

  const IconsFlutterData.fontAwesome5Brands(int codePoint)
      : this(codePoint, "FontAwesome5_Brands");

  const IconsFlutterData.fontAwesome5(int codePoint)
      : this(codePoint, "FontAwesome5");

  const IconsFlutterData.fontAwesomeIcon5(int codePoint)
      : this(codePoint, "FontAwesome5Icon");

  const IconsFlutterData.fontAwesome5Solid(int codePoint)
      : this(codePoint, "FontAwesome5_Solid");

  const IconsFlutterData.enTypo(int codePoint) : this(codePoint, "Entypo");

  const IconsFlutterData.evilIcons(int codePoint) : this(codePoint, "EvilIcons");

  const IconsFlutterData.feather(int codePoint) : this(codePoint, "Feather");

  const IconsFlutterData.foundation(int codePoint)
      : this(codePoint, "Foundation");

  const IconsFlutterData.materialCommunityIcons(int codePoint)
      : this(codePoint, "MaterialCommunityIcons");

  const IconsFlutterData.materialIcons(int codePoint)
      : this(codePoint, "MaterialIcons");

  const IconsFlutterData.octiCons(int codePoint) : this(codePoint, "Octicons");

  const IconsFlutterData.simpleLineIcons(int codePoint)
      : this(codePoint, "SimpleLineIcons");

  const IconsFlutterData.zocial(int codePoint) : this(codePoint, "Zocial");

  const IconsFlutterData.weatherIcons(int codePoint)
      : this(codePoint, "WeatherIcons");

  const IconsFlutterData.brandico(int codePoint) : this(codePoint, "Brandico");

  const IconsFlutterData.elusive(int codePoint) : this(codePoint, "Elusive");

  const IconsFlutterData.fontElico(int codePoint) : this(codePoint, "Fontelico");

  const IconsFlutterData.linearIconsFree(int codePoint)
      : this(codePoint, "LineariconsFree");

  const IconsFlutterData.lineCons(int codePoint) : this(codePoint, "Linecons");

  const IconsFlutterData.maki(int codePoint) : this(codePoint, "Maki");

  const IconsFlutterData.meteoCons(int codePoint) : this(codePoint, "Meteocons");

  const IconsFlutterData.mfgLabs(int codePoint) : this(codePoint, "MfgLabs");

  const IconsFlutterData.modernPictograms(int codePoint)
      : this(codePoint, "ModernPictograms");

  const IconsFlutterData.rpgAwesome(int codePoint)
      : this(codePoint, "RpgAwesome");

  const IconsFlutterData.typicons(int codePoint) : this(codePoint, "Typicons");

  const IconsFlutterData.webSymbols(int codePoint)
      : this(codePoint, "WebSymbols");
}
